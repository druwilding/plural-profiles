<% content_for(:container_class) %>
<% content_for(:title) { "Edit #{@group.name} — Plural Profiles" } %>

<h1>Edit group</h1>

<div class="card">
  <%= render "our/groups/form", group: @group %>
</div>

<p><%= link_to "← Back to group", our_group_path(@group) %></p>
