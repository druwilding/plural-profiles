<% content_for(:container_class) { "narrow" } %>

<h1>Edit Group</h1>

<div class="card">
  <%= render "our/groups/form", group: @group %>
</div>

<p><%= link_to "← Back to group", our_group_path(@group) %></p>
