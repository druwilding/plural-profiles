<% content_for(:container_class) %>
<% content_for(:title) { "Edit #{@profile.name} — Plural Profiles" } %>

<h1>Edit profile</h1>

<div class="card">
  <%= render "our/profiles/form", profile: @profile %>
</div>

<p><%= link_to "← Back to profile", our_profile_path(@profile) %></p>
