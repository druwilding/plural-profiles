= link_to group_profile_path(group.uuid, profile.uuid), class: "profile-card card", "data-action": "click->tree#selectProfileCard", "data-panel-url": panel_group_profile_path(group.uuid, profile.uuid), "data-group-uuid": group.uuid, "data-profile-uuid": profile.uuid do
  - if profile.avatar.attached?
    = image_tag profile.avatar.variant(resize_to_fill: [64, 64]), class: "avatar", width: 64, height: 64, alt: profile.avatar_alt_text.presence || "", loading: "lazy"
  - else
    .avatar.avatar--placeholder
      = render "shared/plural_pride_logo"
  %h3= profile.name
  - if profile.pronouns.present?
    %span.pronouns= profile.pronouns
